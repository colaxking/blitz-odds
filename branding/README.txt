These SVGs preserve the exact approved Blitz Odds artwork by embedding pixel-perfect crops from the original source image. They are visually exact SVG containers, not hand-traced editable vector paths.

format-badge-{straight-up,confidence,survivor,ats}.png and the matching
-icon.png variants are the four pick'em format badges (source: a single
2x2 ChatGPT-generated sheet, cropped per-badge with true alpha
transparency preserved - not a white background). Both the ".png" and the
"-icon.png" are the SAME full icon+wordmark lockup (confirmed as the
correct look via Dan's reference screenshots) - the "-icon.png" is just a
larger export (600px wide) for in-app use where it needs to stay legible
at a bigger display size (league list rows, Create a League format picker,
search result pills), while the plain ".png" is a smaller export (420px
wide) sized for the league-invite email. Do NOT crop these down to
icon-only versions again - an earlier round mistakenly trimmed off the
wordmark banner thinking it was a rendering bug; it wasn't, the full
lockup is the intended look everywhere. Both are pngquant'd.
